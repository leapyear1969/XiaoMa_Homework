# 小马的每日学习 — 每日作业生成器

## 项目简介

本项目是一个基于 Python 的 **每日作业自动生成工具**，提供图形化界面（GUI），可一键生成包含 **语文、数学、英语** 三科的 PDF 作业文档。适用于小学生日常练习。

## 功能概览

### 1. 语文作业（汉字书写练习）

- 从汉字题库（`yuwen.txt`，包含约 3500+ 个常用汉字）中随机抽取汉字
- 生成 **田字格** 书写练习页，每个字配有 **拼音标注**（使用 `pypinyin` 库自动生成带声调的拼音）
- 支持两种难度等级：
  - **普通**：田字格中显示示例字，其余格子留空供学生自行书写
  - **初级**：集成 [MakeMeAHanzi](https://github.com/skishore/makemeahanzi) 笔画数据，逐格递进展示笔画描红，帮助初学者掌握笔顺
- 初级模式通过 `hanzi_metadata.csv` 筛选简单汉字（笔画 ≤ 8、高频、非多音字、基础部首、基础语义）

### 2. 数学作业（加减法练习）

- 自动生成 **10 道随机加减法** 算术题（如 `23 + 45 = `、`78 - 32 = `）
- 支持自定义数值范围上限（默认 100，可在界面中修改，最大 1000）
- 被减数 ≥ 减数，结果不超过设定上限，确保题目合理

### 3. 英语作业（单词书写练习）

- 基于 **牛津 3000 词表**（`oxford-3000.csv`）随机抽取单词
- 使用 **四线三格**（Four-Line）书写练习格式，规范英文书写
- 支持 CEFR 四个难度等级：
  - **A1**：入门级
  - **A2**：基础级
  - **B1**：进阶级
  - **B2**：中高级
- 每个单词显示首个完整彩色示例，其余位置为浅色描红，辅助临摹

### 4. 综合输出

- 一键生成 **三科合一** 的 "每日作业" PDF 文件
- 文件命名格式：`YYYY-MM-DD-每日作业.pdf`
- 支持单独生成某一科目的作业（通过修改代码实现）

---

## 运行环境

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows（推荐 Windows 10/11） |
| Python | 3.7 及以上 |
| 主要依赖 | `reportlab`、`pypinyin`、`tkinter`（Python 自带） |

---

## 安装与运行

### 1. 安装依赖

```bash
pip install reportlab pypinyin
```

### 2. 启动方式

#### 方式一：双击 `start.bat`（Windows）

直接双击项目根目录下的 `start.bat` 即可启动。

#### 方式二：命令行运行

```bash
python homework_app.py
```

### 3. 打包成 EXE（可选）

项目包含 PyInstaller 配置文件 `homework_app.spec`，可使用以下命令打包：

```bash
pip install pyinstaller
pyinstaller homework_app.spec
```

---

## 使用说明

1. 启动程序后，将弹出 **"每日作业生成器"** 窗口
2. 在界面上设置参数：
   - **英语词汇等级**：选择 A1 / A2 / B1 / B2
   - **语文汉字等级**：选择"普通"或"初级"
   - **数学范围上限**：输入数值范围（默认 100）
3. 点击 **"生成全部作业"** 按钮
4. 生成成功后，PDF 文件将保存在项目根目录，系统会弹出路径提示

---

## 项目结构

```
小马的每日学习/
├── homework_app.py          # 主程序（GUI + PDF 生成逻辑，约 1300 行）
├── start.bat                # Windows 一键启动脚本
├── homework_app.spec        # PyInstaller 打包配置
├── yuwen.txt                # 语文汉字题库（3500+ 常用汉字，每行一个）
├── oxford-3000.csv          # 牛津 3000 英语词汇表（含单词、词性、等级）
├── hanzi_metadata.csv       # 汉字元数据（笔画数、频率、部首、语义分类等）
├── makemeahanzi/            # MakeMeAHanzi 笔画数据目录
│   └── graphics.txt         # 汉字 SVG 笔画路径数据（自动下载缓存）
├── archived_homework/       # 历史生成的作业 PDF 存档
├── .git_tmp_prev_hw.py      # 开发辅助脚本
└── 说明文档.md               # 本说明文档
```

---

## 数据文件说明

| 文件 | 说明 |
|------|------|
| `yuwen.txt` | 汉字题库，每行一个汉字，涵盖常用汉字约 3500+ 字 |
| `oxford-3000.csv` | 牛津 3000 核心词汇表，包含 `word`（单词）、`class`（词性）、`level`（CEFR 等级 a1/a2/b1/b2）三个字段 |
| `hanzi_metadata.csv` | 汉字元数据，包含 `char`（汉字）、`strokes`（笔画数）、`freq_rank`（频率排名）、`polyphonic`（是否多音字）、`radical`（部首）、`semantic`（语义分类）六个字段，用于初级模式的筛选 |
| `makemeahanzi/graphics.txt` | MakeMeAHanzi 项目的汉字笔画 SVG 路径数据，约 30MB。程序首次运行时会自动从 GitHub 下载，用于笔画描红功能 |

---

## 技术架构

### 核心技术栈

| 组件 | 技术 |
|------|------|
| GUI 框架 | `tkinter`（Python 标准库） |
| PDF 生成 | `reportlab`（页面布局 + 自定义 Flowable 组件） |
| 拼音生成 | `pypinyin`（带声调的汉字拼音） |
| 笔画数据 | MakeMeAHanzi（SVG 路径解析与渲染） |
| 打包工具 | PyInstaller |

### 自定义 PDF 组件

项目中实现了若干自定义 `reportlab Flowable` 组件：

- **`HanziPracticeRow`**：田字格汉字练习行，支持米字辅助线、拼音标注、笔画逐格递进描红
- **`FourLineWordPractice`**：四线三格英文单词练习，支持彩色示例和浅色描红
- **`EnglishWordPracticeBlock`**：英文单词练习块，整合四线格与词汇信息

### 颜色方案

项目内置了 11 色调色板（`COLOR_PALETTE_HEX`），每个汉字/单词随机分配不同颜色，增强视觉趣味性，适合儿童学习使用。

### 字体策略

程序按优先级自动选择可用字体：
- **中文**：STSong-Light（CID 字体）→ KaiTi / SimKai（系统楷体）→ 回退到 STSong-Light
- **拼音**：优先尝试 Arial / Segoe UI 等西文字体以正确显示声调符号
- **英文**：优先尝试 Comic Sans MS → 回退到 Helvetica

---

## 最近更新记录

参考 Git 提交历史，主要更新包括：

- 初始版本：基础 GUI 和 PDF 生成功能
- 添加 README 文档
- 增加调色板颜色方案，优化视觉效果
- 大版本更新：增加语文（含拼音、初级/普通等级）、数学（可配置范围）和英语（多等级词汇）的完整功能
- 合并分支，整合多模块功能

---

## 许可证

本项目为个人学习工具，用于生成 "小马" 的每日家庭作业。其中使用的第三方资源：
- **Oxford 3000™**：词汇表版权归牛津大学出版社所有
- **MakeMeAHanzi**：笔画数据来源于 [MakeMeAHanzi 开源项目](https://github.com/skishore/makemeahanzi)（MIT License）

---

## 常见问题

### Q: 启动时报错 "缺少依赖 reportlab"

**A:** 请运行 `pip install reportlab` 安装 PDF 生成依赖。

### Q: 启动时报错 "缺少依赖 pypinyin"

**A:** 请运行 `pip install pypinyin` 安装拼音生成依赖。

### Q: 生成的 PDF 中文字体显示异常

**A:** 程序会自动检测 Windows 系统字体目录（`C:/Windows/Fonts`）中的可用字体。请确保系统安装了基本中文字体。

### Q: 笔画描红不显示

**A:** 程序首次使用笔画描红功能时会自动从 GitHub 下载 `graphics.txt` 文件（约 30MB）。请确保网络连接正常。下载后缓存于 `makemeahanzi/` 目录，后续无需重复下载。

### Q: 如何修改生成的题目数量？

**A:** 编辑 `homework_app.py`，修改 `_generate_math_problems(10, math_max)` 中的第一个参数（题目数）和 `_select_chinese_characters(count=5)` 中的 `count` 参数（汉字数），以及 `_select_english_words(level, count=5)` 中的 `count` 参数（单词数）。

---

*最后更新：2026-06-06*
