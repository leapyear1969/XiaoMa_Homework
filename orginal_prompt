📌 总体需求

设计一个 Windows 程序，带 图形化界面（UI），能够 自动生成每日作业清单，并输出为 Word 文档，适合直接打印到 A4 纸 上。
要求排版 区分明确、整洁美观、方便孩子完成作业。

🎨 UI 设计

主界面包含 三个按钮：

【语文】

【数学】

【英语】（旁边有下拉框，选择词汇等级：A1 / A2 / B1 / B2）

点击任意按钮，即生成对应学科的 Word 作业文档（A4 格式）。

📖 功能设计
1. 语文作业

题库文件：yuwen.txt

随机抽取 5 个汉字

展示方式：每行一个汉字，包含：拼音（上方）、描红字（首格）、空格抄写格（后续格子）。

田字格：1.5 cm × 1.5 cm

字体要求：

字体：楷体（推荐“方正楷体”、“田英章楷书”）

字号：42 pt

颜色：#AAAAAA（浅灰色，用于描红）

描红规则：拼音和汉字均为浅灰色；第一个格子带字，其余为空格。

排版：拼音位于格子上沿，汉字居中显示。

2. 数学作业

题目生成规则：随机生成 10 道 100 以内加减法

排版：

每题单独一行

无编号，直接列出，如：23 + 45 =

字号：建议 16–18 pt，行距适中，便于填写答案。

3. 英语作业

题库文件：oxford-3000.csv

词汇选择：根据用户在下拉框选择的等级（A1, A2, B1, B2），随机抽取 4 个单词。

书写格式：每个单词使用 四线三格（Four-line Writing Grid）

四条平行横线：顶线、中虚线、基线、底线

三个带区：上格、中格、下格

字母书写规则：

大写字母 & 高个小写字母（b, d, h, k, l, t）：顶线至基线

短小写字母（a, c, e, m, n, o, r, s, u, v, w, x, z）：中虚线至基线

下探字母（g, j, p, q, y）：基线至底线

描红：单词首行浅灰色打印，后续行空白，便于孩子描写与抄写。

字号：推荐 28–32 pt，保证字母在四线格中清晰对齐。

📄 输出文档要求

输出为 Word（.docx） 文件，自动排版为 A4 页面。

每个学科单独生成对应的 Word 文件，文件名格式：

YYYY-MM-DD-语文作业.docx

YYYY-MM-DD-数学作业.docx

YYYY-MM-DD-英语作业.docx

排版必须：

清晰整洁

各科作业 区分明显（可用标题或分隔线）

保证适合打印，留有足够空白填写空间