请参考下面的 UI 代码和现有项目结构，帮我把“每日作业生成器”增加高级模式。

目标：
1. 保留当前基础模式：
   - 用户点击“生成全部作业”
   - 自动生成语文、数学、英语三科作业
   - 合并为一张 A4 PDF
   - 不能破坏现有功能

2. 增加高级模式：
   - UI 中增加“高级选项”
   - 用户可以勾选要生成的科目：语文、数学、英语
   - 支持单独选择一个或多个科目
   - 支持输出模式：
     a. 每科单独生成一张 A4 PDF
     b. 所选科目合并为一张 A4 PDF
   - 点击“生成所选作业（高级模式）”后，根据选择生成对应 PDF

3. 参数要求：
   - 英语词汇等级继续使用 A1、A2、B1、B2
   - 语文汉字等级继续使用 初级、普通
   - 数学范围上限继续使用用户输入的数字
   - 高级模式生成的内容也要复用这些参数

4. 代码要求：
   - 尽量复用现有 PDF 生成函数
   - 不要重复写三套生成逻辑
   - 建议抽象出：
     - generate_chinese_pdf(...)
     - generate_math_pdf(...)
     - generate_english_pdf(...)
     - merge_pdfs(...)
     - generate_homework(subjects, output_mode, config)
   - 基础模式可以调用 generate_homework(["chinese", "math", "english"], "merged", config)
   - 高级模式可以调用 generate_homework(selected_subjects, output_mode, config)

5. UI 要求：
   - 使用 Tkinter
   - 保持当前简洁风格
   - 基础设置区域保留在顶部
   - 中间保留“生成全部作业（基础模式）”
   - 下方新增“高级选项（单独生成各科作业）”
   - 高级选项中包含：
     - 语文作业 checkbox
     - 数学作业 checkbox
     - 英语作业 checkbox
     - 输出选项 radio button：每科单独 PDF / 合并为一张 PDF
     - 生成所选作业按钮

6. 稳定性要求：
   - 如果高级模式没有选择任何科目，需要弹窗提示
   - 如果数学范围上限不是正整数，需要弹窗提示
   - 生成完成后提示输出文件路径
   - 出错时不要闪退，要弹窗显示错误信息

请先分析项目现有文件结构，再修改代码。修改前先说明你准备改哪些文件，修改后给出完整 diff，并说明如何运行测试。